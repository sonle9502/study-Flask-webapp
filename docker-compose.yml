services:
  frontend:
    build:
      context: ./frontend/my-react-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - mynetwork

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - sqlite_data:/app/data
    environment:
      - FLASK_ENV=development  
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge

volumes:
  sqlite_data:
